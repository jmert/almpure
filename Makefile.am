ACLOCAL_AMFLAGS = -I m4

if USE_MPISTUB
  BUILD_MPISTUB = mpistub
endif

if HAVE_MATLAB
  BUILD_MATLAB = matlab
endif

if HAVE_CYTHON
  BUILD_CYTHON = cython
endif

SUBDIRS = $(BUILD_MPISTUB) s2hat $(BUILD_MATLAB) $(BUILD_CYTHON)

